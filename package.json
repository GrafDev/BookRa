{
  "name": "bookra",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:all": "vite build --mode scratch-click && vite build --mode scratch-auto && vite build --mode wheel-click && vite build --mode wheel-auto",
    "deploy:all": "npm run build:all && firebase deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "dependencies": {
    "gsap": "^3.13.0"
  }
}
